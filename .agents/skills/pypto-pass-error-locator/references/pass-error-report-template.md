## PyPTO Pass 问题分析报告

### 一、基本信息
- **问题标题**: <一句话概述问题>
- **错误级别**: <ERROR/WARN/INFO>
- **复现命令**: `<用户提供的复现命令>`
- **用户代码路径**: `<相关脚本/测试用例路径>`
- **环境信息**: PyPTO版本、Python版本、NPU型号、关键环境变量
- **是否开启调试模式**: <是/否>

### 二、错误位置
- **源码文件**: `<文件路径>`
- **代码行号**: `<行号>`
- **Pass模块**: `<Pass名称>`
- **相关函数/接口**: `<函数名或入口>`
- **定位依据**: 日志中的文件:行号、触发条件、关键分支变量、返回码

### 三、错误信息
#### 3.1 原始错误日志
```text
<原始日志，保留完整错误文本>
```

#### 3.2 核心解析
- **日志级别**: <ERROR/WARN/INFO>
- **文件及行号**: `<file:line>`
- **模块信息**: `<如 NBufferMerge.Config>`
- **错误内容**: <提取后的核心报错信息>

#### 3.3 关联日志/上下文
```text
<多行错误上下文、WARN、堆栈、相邻日志>
```

### 四、错误原因分析
#### 4.1 异常分类
- **异常类型**: <参数配置异常 / 算子约束违反 / 段错误 / 属性缺失 / 计算图成环 / 索引越界 / 其他>
- **分类依据**: <对应日志特征、关键字段、代码行为>

#### 4.2 证据分析
**日志证据**:
- <日志片段 + 说明>

**源码证据**:
- 文件: `<文件路径>`，行号范围: `<start-end>`
- 触发条件、关键分支变量、返回码/异常点

```cpp
<关键源码片段，标注文件名和行号>
```

**计算图/IR证据**（如适用）:
- Before图/After图路径
- 状态异常描述: <图结构变化、IR异常、After缺失等>

**规则/约束证据**:
- 参考文档、违反规则、规则说明

#### 4.3 根因链（必需）
> 完整描述：现象 -> 触发位置 -> 状态异常 -> 违反规则

- **现象（日志）**: <看到的外部报错/中断现象>
- **触发位置（源码切片）**: <具体文件、行号、函数、代码片段>
- **状态异常（计算图/IR变化）**: <Before/After差异、属性缺失、图结构异常等>
- **违反规则（约束文档）**: <具体规则、约束条目、接口要求>

#### 4.4 特殊说明（初始无异常日志场景）
- **首次外显现象**: <pass中断且After图缺失等>
- **补充诊断手段**: <增加异常捕获/临时日志/DumpJsonFile>
- **最终异常证据**: <补充捕获后新增的错误码、错误文本、源码行号>

### 五、修复建议
#### 5.1 修复方案
- <给出明确、具体、可执行的修复方向>

#### 5.2 具体步骤
1. <步骤1：修改哪个文件、哪个逻辑>
2. <步骤2：增加什么校验/判空/映射修正>
3. <步骤3：如何重新编译安装与复现验证>
4. <步骤4：如何确认修复生效>

#### 5.3 验证方式
- <验证命令>
- <预期日志/图/IR结果>

### 六、附录
- **完整日志路径**: `<日志文件路径>`
- **相关源码文件**: `<文件路径>`
- **计算图/IR文件**: `<Before图路径>、<After图路径>、<IR路径>`
- **相关文档**: `<约束文档路径>`
