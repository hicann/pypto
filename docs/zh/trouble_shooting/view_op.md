# 视图类OP 错误码

（待补充）

- **范围**：FC9XXX
- 本文档说明视图类 OP 的错误码定义、场景说明与排查建议。
- 补充错误码时，可注明 **关联 Skill**（链接至 [.agents/skills](../../../.agents/skills) 下对应技能，便于排查时加载）。


## 错误码定义

相关错误码的枚举与码值统一定义在 `framework/include/tilefwk/error_code.h`（与其它 OPERATION 子类共用同一头文件中的枚举分区）。
